[project]
name = "backend"
version = "0.1.0"
description = "FastAPI backend for CareerLift"
authors = [{name = "Ty"}]
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.7.0",
    "pydantic-settings>=2.2.0",
]

[tool.setuptools]
packages = ["app"]
